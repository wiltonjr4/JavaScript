/*
CHALLENGE

Write a program that, given the desired pressure entered by the driver
and the tire pressure read by the pump, indicates the difference
between the desired pressure and the pressure read.
*/

const value1 = parseInt(gets());
const value2 = parseInt(gets());

print(value1 - value2);